#include <bits/stdc++.h>

using namespace std;
// Some typedef's
typedef long long ll;
typedef unsigned long long ull;
const int inf = 1e9 + 7;
const double eps = 1e-6;
const double pi = 1.00 * acos(-1.00);
#define IOS                  \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(0);
typedef long long ll;
int main()
{
    IOS;
    int t;
    cin >> t;
    while (t--)
    {
        int n,k;
        cin>>n>>k;
        string s;
        cin>>s;
        vector<int>mp_small(26,0),mp_big(26,0);
        for(char c : s){
            if(c - 'a'>=0)
                vec_small[c-'a']++;
            else if(c-'a'<0){
                vec_big[c-'a']++;
            }
        }
        

        a - 4
        A - 2
        b - 1
        B - 1
        c - 1
        C - 1
        E - 1
    }

    return 0;
}